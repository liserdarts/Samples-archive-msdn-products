<common:LayoutAwarePage
    x:Name="pageRoot"
    x:Class="UCourseSelector.MainPage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UCourseSelector"
    xmlns:common="using:UCourseSelector.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <Page.Resources>
        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">Session Picker Demo App</x:String>
    </Page.Resources>
    
    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Style="{StaticResource LayoutRootStyle}">

 <!-- BEGIN -->

        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="40" />
            <RowDefinition Height="100"/>
            <RowDefinition Height="80"/>
            <RowDefinition Height="40" />
            <RowDefinition Height="*"/>
            <RowDefinition Height="150" />
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:ConnectionId='10' x:Name="backButton" Grid.Column="0"                IsEnabled="{Binding Frame.CanGoBack, ElementName=pageRoot}" Style="{StaticResource BackButtonStyle}"/>
            <TextBlock x:Name="pageTitle" Grid.Column="1" Text="{StaticResource AppName}" Style="{StaticResource PageHeaderTextStyle}"/>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
            </Grid.ColumnDefinitions>
            <Button x:ConnectionId='8' Grid.Column="1" Content="Signin to get Session List" x:Name="loginButton"                    HorizontalAlignment="Right" />
            <Button x:ConnectionId='9' Grid.Column="1" Visibility="Collapsed" Content="Signout" x:Name="logoutButton"                     HorizontalAlignment="Right" />
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="1" x:Name="UserProfile">
                <TextBlock x:Name="DisplayName" Text="{Binding}" FontWeight="Bold" FontSize="20" />
                <TextBlock x:Name="Mail" Text="{Binding}" FontStyle="Italic" FontSize="16" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="SessionListTitle" Grid.Column="1" Text="Sessions:" Style="{StaticResource PageHeaderTextStyle}"/>
            <TextBlock x:Name="CalendarTitle" Grid.Column="2" Text="{Binding}" Style="{StaticResource PageHeaderTextStyle}"/>
        </Grid>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Grid.Column="1">
                <Button x:ConnectionId='6' Visibility="Collapsed" Content="Refresh Sessionlist" x:Name="refreshButton"                            HorizontalAlignment="Left" />
                <Button x:ConnectionId='7' Visibility="Collapsed" Content="Add-to or Update Calendar" x:Name="addCalButton"                           HorizontalAlignment="Right"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Column="2">
                <Button x:ConnectionId='4' Visibility="Collapsed" Content="Remove from Calendar" x:Name="removeCalButton"                              />
                <Button x:ConnectionId='5' Visibility="Collapsed" Content="Share with Friends" x:Name="sendMailButton"                             />
            </StackPanel>
            <Popup x:Name="ParentedPopup" HorizontalOffset="800" VerticalOffset="50"  IsLightDismissEnabled="True">
                <Popup.ChildTransitions>
                    <TransitionCollection>
                        <PaneThemeTransition />
                    </TransitionCollection>
                </Popup.ChildTransitions>
                <Border BorderBrush="{StaticResource ApplicationForegroundThemeBrush}" BorderThickness="2" Background="{StaticResource ApplicationPageBackgroundThemeBrush}" x:Name="RootPopupBorder">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Margin="10" Grid.Column="0" Grid.Row="0" Text="To:" Style="{StaticResource BaselineTextStyle}" />
                        <TextBox Margin="10" Grid.Column="1" Grid.Row="0" Name="ToBox" Width="400"/>
                        <TextBlock Margin="10" Grid.Column="0" Grid.Row="1" Text="Subject:" Style="{StaticResource BaselineTextStyle}" />
                        <TextBox Margin="10" Grid.Column="1" Grid.Row="1" Name="SubjectBox" Width="400"/>
                        <TextBlock Margin="10" Grid.Column="0" Grid.Row="2" Text="Body:" Style="{StaticResource BaselineTextStyle}" />
                        <TextBox Margin="10" Grid.Column="1" Grid.Row="2" Name="BodyBox" Height ="100" Width="400"/>
                        <StackPanel Margin="10" Grid.Column="1" Grid.Row="3" Grid.ColumnSpan="2" HorizontalAlignment="Center" Orientation="Horizontal">
                            <Button x:ConnectionId='3' HorizontalAlignment="Center" Content="Send"                         />
                        </StackPanel>
                    </Grid>
                </Border>
            </Popup>
        </Grid>

        <Grid Grid.Row="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
            </Grid.ColumnDefinitions>
            <Grid.Resources>
                <CollectionViewSource x:Name="EventsInfoCollection" IsSourceGrouped="true"/>
                <CollectionViewSource x:Name="SessionCollection" IsSourceGrouped="true"/>
            </Grid.Resources>

            <ListView x:ConnectionId='1' Grid.Column="1" x:Name="SessionListView" Width="600" 
                VerticalAlignment="Stretch" HorizontalAlignment="Left" 
                CanReorderItems="True"
                CanDragItems="True" 
                                                    
                ScrollViewer.VerticalScrollMode="Enabled"  ScrollViewer.VerticalScrollBarVisibility="Auto"
                ItemTemplate="{StaticResource SessionListTemplate}"
                ItemsSource="{Binding Source={StaticResource SessionCollection}}"
                                                                   
            />
 
            <ListView x:ConnectionId='2' Grid.Column="2" x:Name="EventsListView" Width="600" 
                VerticalAlignment="Stretch" HorizontalAlignment="Left"
                ScrollViewer.VerticalScrollMode="Enabled"  ScrollViewer.VerticalScrollBarVisibility="Auto"
                CanReorderItems="True" 
                CanDragItems="False"
                AllowDrop="True"
                                     
                ItemTemplate="{StaticResource CalendarViewTemplate}"
                ItemsSource="{Binding Source={StaticResource EventsInfoCollection}}" 
            />
        </Grid>
        
        <!-- END-->
        
        
        <VisualStateManager.VisualStateGroups>

            <!-- Visual states reflect the application's view state -->
            <VisualStateGroup x:Name="ApplicationViewStates">
                <VisualState x:Name="FullScreenLandscape"/>
                <VisualState x:Name="Filled"/>

                <!-- The entire page respects the narrower 100-pixel margin convention for portrait -->
                <VisualState x:Name="FullScreenPortrait">
                    <Storyboard>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="backButton" Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource PortraitBackButtonStyle}"/>
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>

                <!-- The back button and title have different styles when snapped -->
                <VisualState x:Name="Snapped">
                    <Storyboard>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="backButton" Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource SnappedBackButtonStyle}"/>
                        </ObjectAnimationUsingKeyFrames>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="pageTitle" Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource SnappedPageHeaderTextStyle}"/>
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</common:LayoutAwarePage>

