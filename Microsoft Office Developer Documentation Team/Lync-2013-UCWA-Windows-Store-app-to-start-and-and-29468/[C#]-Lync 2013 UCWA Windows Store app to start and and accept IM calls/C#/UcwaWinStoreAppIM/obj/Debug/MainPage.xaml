<Page
    x:Name="pageRoot"
    x:Class="WinStoreUcwaAppIM.MainPage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinStoreUcwaAppHello"
    xmlns:common="using:WinStoreUcwaAppHello.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">My UCWA RT App</x:String>
    </Page.Resources>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Text="{StaticResource AppName}" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>
        </Grid>
        <StackPanel HorizontalAlignment="Left" Height="535" Margin="581,43,0,0" Grid.Row="1" VerticalAlignment="Top" Width="414">
            <TextBox x:Name="textBlockStatus" TextWrapping="Wrap" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="22,30,0,0" MinWidth="160" MinHeight="60" Height="173" Width="371" FontSize="14"/>
            <TextBox x:Name="textBlockEvents" HorizontalAlignment="Left" Margin="22,20,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="289" Width="371"/>
        </StackPanel>
        <StackPanel HorizontalAlignment="Left" Height="535" Margin="128,43,0,0" Grid.Row="1" VerticalAlignment="Top" Width="448">
            <TextBlock x:Name="textBlockIntro" TextWrapping="Wrap" Text="TextBlock" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,20" FontSize="16"/>
            <TextBlock x:Name="textBlockUserName" TextWrapping="Wrap" Text="User name (e.g., john@contoso.com)" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="20,0" ToolTipService.ToolTip="A Lync user account" FontSize="16"/>
            <TextBox x:ConnectionId='0' x:Name="textBoxUserName" TextWrapping="Wrap" ToolTipService.ToolTip="Lync user account name" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,0,0,20" Width="250"                                />
            <TextBlock x:Name="textBlockPassword" TextWrapping="Wrap" Text="Password:" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,0,0,0" FontSize="16"/>
            <PasswordBox x:ConnectionId='0' x:Name="passwordBox" PlaceholderText="" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,0,0,20" Width="250"                                    />
            <CheckBox x:ConnectionId='0' x:Name="checkBoxSaveUserInfo" Content="Save user info" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,0,0,0" FlowDirection="RightToLeft"                                                                  />
            <CheckBox x:ConnectionId='0' x:Name="checkBoxUseWindowsAuth" Content="Use Windows Auth" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,0,0,0" FlowDirection="RightToLeft"                                                                      />
            <Button x:ConnectionId='0' x:Name="buttonSignIn" Content="Sign in" HorizontalAlignment="Left" VerticalAlignment="Top" Width="84" Height="40" Margin="20,0,0,0" RenderTransformOrigin="1.056,0.254" FontSize="16"                             />
            <Button x:ConnectionId='0' x:Name="ButtonShowMe" Content="me" HorizontalAlignment="Left" VerticalAlignment="Top" Width="84" Height="40" Margin="100,-40,0,0"                           />
            <Button x:ConnectionId='0' x:Name="ButtonComm" Content="Comm" HorizontalAlignment="Left" VerticalAlignment="Top" Width="84" Height="40" Margin="180,-40, 0,0"                          />
        </StackPanel>
    </Grid>
</Page>

